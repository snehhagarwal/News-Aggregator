import axios from "axios";
const URL = "http://localhost:8000";
export const loginUser = async (data) => {
  try {
    console.log(data);
    // return res;
  } catch (error) {
    return error;
  }
};
